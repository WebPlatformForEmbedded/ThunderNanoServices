{
  "$schema": "plugin.schema.json",
  "info": {
    "title": "VideoPlaybackChecker Plugin",
    "version": "1.0",
    "callsign": "VideoPlaybackChecker",
    "locator": "libWPEFrameworkVideoPlaybackChecker.so",
    "status": "alpha",
    "description": "The VideoPlaybackChecker allows exchanging text messages between users gathered in virtual rooms. The rooms are dynamically created and destroyed based on user attendance. Upon joining a room the client receives a unique token (room ID) to be used for sending and receiving the messages."
  },
  "interface": {
    "$ref": "{interfacedir}/VideoPlaybackCheckerAPI.json#"
  }
}
